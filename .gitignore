.DS_Store
__pycache__/
*.py[cod]

.idea/

# Cover datasets (large, external)
ALASKA_v2_TIFF_512_GrayScale_10K/
ALASKA_v2_TIFF_512_GrayScale_50/

# Ad-hoc embedding output dirs
output/
out/

# Bulky deterministic artifacts (re-derivable via embedding.py, seed 12345).
# CSV/PNG/JSON results under experiments/ ARE tracked.
experiments/**/steganograms/

# 10K-campaign raw per-image tables: deterministic re-derivable outputs that
# exceed GitHub's 100 MB file limit (probe_raw ~187 MB each, per_image ~297 MB).
# Summaries/metadata/figures of those runs ARE tracked.
experiments/probing_10k_*/probe_raw.csv
experiments/distribution_benchmark_10k_*/per_image.csv

# DESSERT-2026 campaign raw per-image tables (re-derivable, > 100 MB) and the smoke run
experiments/dessert2026*/benchmark/per_image.csv
experiments/dessert2026*/probing_*/probe_raw.csv
experiments/dessert2026_smoke/
