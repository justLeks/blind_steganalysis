# Blind steganalysis — pinned dependencies (verified on Python 3.11, 2026-07).
# Install:  python3 -m pip install -r requirements.txt

numpy==2.4.2
pillow==12.1.1
conseal==2025.11        # HUGO/MiPOD simulators + selection-channel probabilities
numba==0.64.0           # imported by embedding.py (JIT cache workaround); also a conseal dependency
scipy==1.17.1           # ndimage convolutions for the score-map registry; also a conseal dependency
matplotlib==3.10.6      # grayscale comparison figures (make_comparison_figures.py)

# Tests run on the stdlib `unittest` runner — no extra dependency needed:
#   python3 -m unittest discover -s tests -v
